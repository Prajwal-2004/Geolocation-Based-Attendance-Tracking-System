import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.geolocation',
  appName: 'Geolocation Based Attendance Tracking System',
  webDir: 'dist'
};

export default config;
